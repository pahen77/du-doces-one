// Configuration for Du Doces E-commerce
window.CONFIG = {
  // API_BASE will be automatically set based on the current domain when deployed
  // For Railway deployment, both frontend and backend are served from the same domain
  API_BASE: window.location.origin, // Uses current domain (Railway will handle this)
  USE_API: true,
  // ChatVolt AI integration key
  CHATVOLT_KEY: "cmfk2a6ko0w5htglbf0n10833"
};